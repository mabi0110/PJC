#include <iostream>

using namespace std;

/*
int main() {
    int userInput;
    int tempUserInput = 0;

    int tempCounter = 0;
    int maxCounter = 0;

    int maxNumber = 0;
    int tempMaxNumber = 0;

    do {

        cin >> userInput;
       
        if (userInput != tempUserInput) {
            tempCounter = 0;
            tempUserInput = userInput;
        }

        if (tempUserInput != 0) {
            tempCounter++;
            tempMaxNumber = userInput;
        }

        if (tempCounter > maxCounter) {
            maxCounter = tempCounter;
            maxNumber = tempMaxNumber;
        }

    } while (userInput != 0);

	cout << "Longest sequence : " << maxCounter << " times " << maxNumber;
}
*/