#include <iostream>
#include <string>

using namespace std;

/*
int main() {
	int userInput, userInputTemp, sumOfDigitsTemp, max;
	int sumOfDigits = 0;

	do {
		cout << "enter a natural number (0 if done): ";
		cin >> userInput;
		userInputTemp = userInput;
		sumOfDigitsTemp = 0;
		while (userInputTemp > 0) {
			sumOfDigitsTemp +=  userInputTemp % 10;
			userInputTemp /= 10;
		}
		if (sumOfDigitsTemp > sumOfDigits) {
			sumOfDigits = sumOfDigitsTemp;
			max = userInput;
		}
	} while (userInput != 0);

	cout << "Max sum of digits was " << sumOfDigits << " for " << max << endl;
}
*/
