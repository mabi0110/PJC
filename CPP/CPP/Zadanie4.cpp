#include <iostream>
#include <string>
using namespace std;

/*
int main() {
	int a, b, c, max;
	string sign;

	cout << "Enter three non-negative numbers: ";
	cin >> a;
	cin >> b;
	cin >> c;

	max = (a > b && a > c) ? a : (b > a && b > c) ? b : c;

	for (int i = max; i > 0; i--) {
		for (int j = 0; j < 3; j++) {

			sign = ((j == 0 && i <= a) | (j == 1 && i <= b) | (j == 2 && i <= c)) ? "*" : " ";
			cout << sign;
		
			//sign = (j == 0 && i <= a) ? "*" : " ";
			//cout << sign;
			//sign = (j == 1 && i <= b) ? "*" : " ";
			//cout << sign;
			//sign = (j == 2 && i <= c) ? "*" : " ";
			//cout << sign;
			
			
			//if ((j == 0 && i <= a) | (j == 1 && i <= b) | (j == 2 && i <= c)) {
			//	cout << "*";
			//}
			//else {
			//	cout << " ";
			//}
			
		}
		cout << endl;
	}
}
*/

